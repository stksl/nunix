nunix (not unix) - is a VM built on top of Linux x86-64.
Implements basic standarts that are being used in modern computing.
(of course using Linux kernel API as a workaround)

Dependencies:
Linux x86-64 operating system with .NET 7+ SDK installed